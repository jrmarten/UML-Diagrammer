Just to remember what settings do
and where they are in the code as to not forget them.

templateDir: 
	#			sets the directory for the templates
	${code}		UMLApplicationModel.UMLMenuBuilder.getTemplates()
	
Background:
	#			sets the background to to an integer value
	${code}		UMLView.createDrawing( )
	
	